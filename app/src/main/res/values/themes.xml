<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Activity shell theme only: no action bar, transparent system bars (Compose draws
         edge-to-edge), and a themed window background to avoid a colour flash before the
         first Compose frame. The real colours come from HermesTheme in :core:ui. -->
    <style name="Theme.Hermes" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
