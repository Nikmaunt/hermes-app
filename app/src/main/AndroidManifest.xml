<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Security posture, present from the first :app commit and never loosened without an
      explicit audit (decision-log D32):
        - allowBackup=false + empty dataExtractionRules (cloud-backup AND device-transfer)
          => private data never leaves the device via backup or phone-to-phone transfer.
        - usesCleartextTraffic=false + network_security_config => TLS only, system CAs only.
      Every future exported component (share target M2, notification listener M8) is added
      under its own audit, not by inertia.
    -->
    <application
        android:name=".HermesApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Hermes"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Hermes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
